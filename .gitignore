# Secretos — NUNCA subir
api_key.txt

# Entorno y caché
.venv/
__pycache__/
*.pyc

# Estado personal (memoria, voz, personalidad del usuario)
forjis_state.json

# Modelos descargados (los baja install.bat / download_models.py)
models/*.onnx
models/*.onnx.json
models/*.task

# Temporales
_*.txt
_*.wav
_forjis_say.wav
